// Test file to check if @/ alias is working
console.log('@/ alias test: This is a test file to check if the @/ alias is working correctly');

export const testFunction = () => {
  return '@/ alias is working!';
};
